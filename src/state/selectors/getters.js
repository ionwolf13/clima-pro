// Zustand Selectors
export const selectCurrentLocation = (state) => state.currentLocation;
export const selectFavoriteLocations = (state) => state.favoriteLocations;
export const selectTemperatureUnit = (state) => state.temperatureUnit;
